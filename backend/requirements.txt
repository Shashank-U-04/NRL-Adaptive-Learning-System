# ── Web Framework ──────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# ── Database (PostgreSQL async; SQLite for tests) ──────────
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
aiosqlite==0.20.0
alembic==1.14.0

# ── Auth ──────────────────────────────────────────────────
PyJWT==2.9.0

# ── Validation & Settings ─────────────────────────────────
pydantic[email]==2.10.4
python-dotenv==1.0.1

# ── ML / RL Engine (optional, lazy-loaded) ────────────────
# Comment out on tiny machines — RL falls back to heuristic engine.
torch==2.5.1; sys_platform != "win32" or python_version < "3.13"
numpy==2.2.5

# ── Testing ───────────────────────────────────────────────
pytest==8.3.4
pytest-asyncio==0.24.0
